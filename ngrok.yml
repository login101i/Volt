version: "2"
authtoken: # Add your ngrok authtoken here (or set via: ngrok config add-authtoken YOUR_TOKEN)

tunnels:
  frontend:
    addr: 3000
    proto: http
    schemes: [https]  # Force HTTPS
    
  backend:
    addr: 5000
    proto: http
    schemes: [https]  # Force HTTPS












